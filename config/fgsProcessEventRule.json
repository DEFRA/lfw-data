{
  "Description": "Event rule to schedule the fgsProcess lambda execution",
  "Name": "{{stage}}lfpFgsProcessRule",
  "RoleArn": "{{lambdaRoleArn}}",
  "ScheduleExpression": "cron(16 * * * ? *)",
  "State": "ENABLED"
}
