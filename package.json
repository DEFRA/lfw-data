{
  "name": "lfw-data",
  "version": "0.1.0",
  "description": "Serverless (AWS Lambda) project for LFW data processing",
  "scripts": {
    "deploy": "sls deploy -v --force",
    "integration-test": "lab test/integration -v -r console -o stdout -r junit -o test/output-integration.xml -m 30000",
    "lint": "standard",
    "unit-test": "lab test/unit -c -r console -o stdout -r lcov -o lcov.info -r junit -o test/output-unit.xml",
    "post-deployment-test": "npm run integration-test",
    "pre-deployment-test": "npm run lint && npm run unit-test",
    "test": "npm run lint && npm run unit-test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DEFRA/lfw-data.git"
  },
  "author": "DEFRA",
  "license": "OGL",
  "bugs": {
    "url": "https://github.com/DEFRA/lfw-data/issues"
  },
  "homepage": "https://github.com/DEFRA/lfw-data#readme",
  "devDependencies": {
    "code": "5.2.1",
    "codeclimate-test-reporter": "0.5.1",
    "lab": "16.1.0",
    "standard": "12.0.1"
  },
  "dependencies": {
    "aws-sdk": "2.331.0",
    "csvtojson": "2.0.8",
    "joi": "13.7.0",
    "moment-timezone": "0.5.21",
    "pg": "7.5.0",
    "proxy-agent": "3.0.3",
    "serverless": "1.32.0",
    "sinon": "6.3.5",
    "sql": "0.78.0",
    "xml2js": "0.4.19"
  }
}
